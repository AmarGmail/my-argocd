apiVersion: apps/v1
kind: Deployment

metadata:
  name: nginx-deployment
  labels: nginx

spec:
  replicas: 1

  selector:
    matchLabels:
      app: nginx

  template:
    metadata:
      labels:
        app: nginx

    spec:
      container:
        - name: nginx
          image: nginx
